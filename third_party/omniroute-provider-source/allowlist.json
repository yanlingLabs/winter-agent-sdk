{
  "$comment": "Extractor INPUT contract (WS-13 §3/§13; OmniRoute report §11.2). Hand-maintained and reviewed, in two halves. (1) `paths` is the VERSIONED path allowlist the sparse checkout materializes — the extractor refuses to materialize anything outside it and never widens the boundary on its own. (2) `providers` is the reviewed provider intake list: a row's `risk` is what the extractor stamps onto the generated WinterProviderDescriptor, and promoting one to \"approved\" is a deliberate, reviewed edit. No upstream source is vendored under this directory beyond the upstream LICENSE/NOTICE copies the extraction manifest records — see README.md.",
  "allowlistVersion": 3,
  "upstreamProject": "OmniRoute",
  "importBoundary": {
    "$comment": "WS-13 §3 step 2 says an import outside the allowlist \"fails the job for review; the boundary never auto-widens\". Taken as a rule about WIDENING, not about aborting: the literal extractor never follows an import at all, so a value that would have to come from an unmaterialized module is DROPPED with a recorded rejection (`unresolved-reference`), and the offending specifier is listed in the ledger's `outOfAllowlistImports` so drift stays visible. Aborting instead would make the pipeline unrunnable by construction — `open-sse/config/providers/shared.ts` imports a dozen modules outside any reviewable provider-data boundary. The boundary keeps its teeth through `failOnUnresolvedFields`: an allowlisted provider whose IDENTITY-critical field cannot be resolved from materialized files fails the run for review.",
    "resolveIdentifiersWithin": "materialized-allowlisted-files-only",
    "failOnUnresolvedFields": [
      "id",
      "format",
      "executor",
      "authType"
    ]
  },
  "paths": [
    {
      "pattern": "/LICENSE",
      "role": "notice",
      "why": "OmniRoute root MIT licence — copied into third_party/ and registered in the extraction manifest (report §12)."
    },
    {
      "pattern": "/THIRD_PARTY_NOTICES.md",
      "role": "notice",
      "why": "Upstream's own third-party notices — copied beside the licence so a reader of Winter's NOTICE can reach upstream's provenance record."
    },
    {
      "pattern": "/package.json",
      "role": "claim",
      "why": "The upstream package version at the pin. Read for the provenance record only; no descriptor field is derived from it."
    },
    {
      "pattern": "/README.md",
      "role": "claim",
      "why": "One of the two upstream provider-COUNT claim sources. Read only to compute the denominator discrepancy WS-13 §3 step 5 requires the extractor to report; never a descriptor input."
    },
    {
      "pattern": "/docs/reference/PROVIDER_REFERENCE.md",
      "role": "claim",
      "why": "The generated provider reference — the second count claim source, and the per-category denominator. Report §5 warns its header is not a release authority, so it is read as a CLAIM to be checked against the enumerated tree, never as truth."
    },
    {
      "pattern": "/open-sse/config/providerPluginManifest.ts",
      "role": "extract",
      "why": "The upstream-supported JSON-safe projection boundary (report §5) — read for its own reviewed field list."
    },
    {
      "pattern": "/open-sse/config/providerPluginManifestRegistry.ts",
      "role": "extract",
      "why": "Companion to the manifest projection."
    },
    {
      "pattern": "/open-sse/config/providerPluginManifestUrl.ts",
      "role": "extract",
      "why": "Companion to the manifest projection."
    },
    {
      "pattern": "/open-sse/config/providers/**",
      "role": "extract",
      "why": "The backend registry (report §3.2): the execution-shaped provider entries and their static model rows. This is the main descriptor input."
    },
    {
      "pattern": "/src/shared/constants/providers.ts",
      "role": "extract",
      "why": "The product-catalog barrel (report §3.1) — category membership and display metadata."
    },
    {
      "pattern": "/src/shared/constants/providers/**",
      "role": "extract",
      "why": "The per-category product-catalog literals. These carry the category each upstream id belongs to, which is what WS-13 §1's disposition table keys on."
    },
    {
      "pattern": "/src/lib/providerModels/modelDiscovery.ts",
      "role": "extract",
      "why": "The live-discovery normalizer (report §3.3) — read for the discovery response shapes Winter's own bounded discovery must accept."
    },
    {
      "pattern": "/src/lib/db/migrations/*retire*.sql",
      "role": "extract",
      "why": "Retirement migrations for GPL-derived and provenance-hold integrations (report §12). ZERO MATCHES AT THIS PIN — see PROVENANCE.md; the pattern stays so a future bump picks them up."
    },
    {
      "pattern": "/src/lib/db/migrations/*provenance*.sql",
      "role": "extract",
      "why": "Provenance-hold migrations. Zero matches at this pin, kept for the same reason."
    }
  ],
  "providers": [
    {
      "upstreamId": "openai",
      "winterId": "openai",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://developers.openai.com/api/docs/pricing",
        "tier": "fetched-document"
      },
      "adapterIdOverride": "winter.openai-responses",
      "risk": {
        "class": "approved",
        "reasons": []
      },
      "$comment": "`adapterIdOverride` because upstream's `format: \"openai\"` names its own DEFAULT execution path (Chat Completions), not the surface Winter drives. Six of upstream's own openai rows carry `targetFormat: \"openai-responses\"` — the *-pro and GPT-5.6 families are responses-ONLY, and upstream says so per model while its provider-level `format` does not — so deriving the adapter from `format` produced responses-only rows under a Chat Completions adapter. The overlay row has said `winter.openai-responses` since the seed, which is exactly what hid it: the merged catalog was consistent while the layer `--offline` validates STANDALONE was not. Found by extending that gate to the standalone layer (review r2 minor 5), which is the same shadow class as the Vertex adapter misroute."
    },
    {
      "upstreamId": "anthropic",
      "winterId": "anthropic",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://claude.com/pricing",
        "tier": "fetched-document"
      },
      "risk": {
        "class": "approved",
        "reasons": []
      }
    },
    {
      "upstreamId": "gemini",
      "winterId": "google",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://ai.google.dev/gemini-api/docs/pricing",
        "tier": "fetched-document"
      },
      "risk": {
        "class": "approved",
        "reasons": []
      },
      "$comment": "The upstream id is `gemini`, not `google` — allowlistVersion 1 named `google` on both sides, which matched no upstream row at v3.8.50 and would have silently produced no google extraction at all. `winterId` stays `google` because WS-13 §12 and every seeded row use it."
    },
    {
      "upstreamId": "openrouter",
      "winterId": "openrouter",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://openrouter.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026; the api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://openrouter.ai/api/v1. EVIDENCE TIER: pinned-upstream — see PROVENANCE.md \"Two tiers of admission citation\". The audit (docs/research/Provider-third-party-access-audit.md) covers the OAuth/keyless ids and carries NO citation for this one, so its 51 references cannot upgrade this row.",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "gateway: capability parity is model-by-model (WS-13 §14.4); per-request provider/data-policy controls must be preserved"
        ]
      }
    },
    {
      "upstreamId": "deepseek",
      "winterId": "deepseek",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://platform.deepseek.com — the vendor's own developer platform as OmniRoute's product catalog records it at 5458026; the api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Responses at https://api.deepseek.com. EVIDENCE TIER: pinned-upstream — see PROVENANCE.md \"Two tiers of admission citation\". The audit covers the OAuth/keyless ids and carries no citation for this one.",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "reasoning continuity is tool-loop fragile (WS-13 §8.2: missing reasoning state mid-tool-loop is a hard error)"
        ]
      }
    },
    {
      "upstreamId": "azure-openai",
      "winterId": "azure-openai",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "cloud-credential",
        "citation": "https://azure.microsoft.com/products/ai-services/openai-service — the vendor's own product page as OmniRoute's product catalog records it at 5458026. The credential is the OPERATOR'S OWN Azure subscription key against their own resource endpoint, which is why the basis is cloud-credential rather than api-key. EVIDENCE TIER: pinned-upstream — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "native cloud: deployment/api-version identity and Entra auth need their own proof (WS-13 §5)"
        ]
      },
      "$comment": "PRODUCT-CATALOG ONLY at this pin: `azure-openai` has a row in src/shared/constants/providers/apikey/enterprise-cloud.ts but NO backend RegistryEntry, so upstream contributes its identity and nothing executable. Its endpoints and auth are Winter-authored in the overlay; the rejection ledger records the absence."
    },
    {
      "upstreamId": "bedrock",
      "winterId": "bedrock",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "cloud-credential",
        "citation": "https://aws.amazon.com/bedrock — the vendor's own product page as OmniRoute's product catalog records it at 5458026. The credential is the OPERATOR'S OWN AWS account, signed with SigV4, which is why the basis is cloud-credential rather than api-key. EVIDENCE TIER: pinned-upstream — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "native cloud: SigV4 signing + event-stream decoding need their own proof (WS-13 §5)"
        ]
      }
    },
    {
      "upstreamId": "vertex",
      "winterId": "vertex",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "cloud-credential",
        "citation": "https://cloud.google.com/vertex-ai — the vendor's own product page as OmniRoute's product catalog records it at 5458026. The credential is the OPERATOR'S OWN GCP project via Application Default Credentials, which is why the basis is cloud-credential rather than api-key. EVIDENCE TIER: pinned-upstream — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "initialModelStatus": "experimental",
      "adapterIdOverride": "winter.vertex-gemini",
      "risk": {
        "class": "review-required",
        "reasons": [
          "native cloud: project/location auth and partner-protocol differences need their own proof (WS-13 §5)"
        ]
      },
      "$comment": "`adapterIdOverride` is Lane B's `VERTEX_ADAPTER_ID`, read from p6/lane-b's adapters/google/vertex.ts. Without it the mapper derives the adapter from the PROTOCOL, and Vertex shares GenerateContent with the Gemini API — so the extracted row named `winter.google-generate-content` and a registry, which resolves BY ID, would have served a Vertex session from the Gemini API adapter: no location-scoped URL, no ADC credential. The overlay row was corrected first; this fixes the layer `--offline` validates STANDALONE, which is exactly the layer whose defects an overlay shadow hides. `initialModelStatus: \"experimental\"` per R6-16 — the native-cloud families enter the catalog as `experimental`, and Lane B's `winter.vertex-gemini` adapter is live. Absent this field a row is `candidate`, which is what every other provider gets: upstream presence promotes nothing."
    },
    {
      "upstreamId": "agentrouter",
      "winterId": "agentrouter",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://agentrouter.org — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", Anthropic Messages at https://agentrouter.org. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — Anthropic Messages dialect, 3 seeded model row(s), api root https://agentrouter.org. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "agnes",
      "winterId": "agnes",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://agnes-ai.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://apihub.agnes-ai.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 3 seeded model row(s), api root https://apihub.agnes-ai.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "ai21",
      "winterId": "ai21",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://www.ai21.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.ai21.com/studio/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.ai21.com/studio/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "aimlapi",
      "winterId": "aimlapi",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://aimlapi.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.aimlapi.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 6 seeded model row(s), api root https://api.aimlapi.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "alibaba",
      "winterId": "alibaba",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://bailian.console.alibabacloud.com/ — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://dashscope-intl.aliyuncs.com/compatible-mode/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 14 seeded model row(s), api root https://dashscope-intl.aliyuncs.com/compatible-mode/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "alibaba-cn",
      "winterId": "alibaba-cn",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://dashscope.console.aliyun.com/ — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://dashscope.aliyuncs.com/compatible-mode/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 14 seeded model row(s), api root https://dashscope.aliyuncs.com/compatible-mode/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "ant-ling",
      "winterId": "ant-ling",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://developer.ant-ling.com/en/docs/ — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.ant-ling.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 3 seeded model row(s), api root https://api.ant-ling.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "api-airforce",
      "winterId": "api-airforce",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://api.airforce — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.airforce/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 7 seeded model row(s), api root https://api.airforce/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "bai",
      "winterId": "bai",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://b.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.b.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.b.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "baichuan",
      "winterId": "baichuan",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://www.baichuan-ai.com/ — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.baichuan-ai.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 5 seeded model row(s), api root https://api.baichuan-ai.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "baidu",
      "winterId": "baidu",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://ernie.baidu.com/ — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://qianfan.baidubce.com/v2. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 16 seeded model row(s), api root https://qianfan.baidubce.com/v2. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "baseten",
      "winterId": "baseten",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://baseten.co — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://inference.baseten.co/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://inference.baseten.co/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "bazaarlink",
      "winterId": "bazaarlink",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://bazaarlink.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://bazaarlink.ai/api/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 33 seeded model row(s), api root https://bazaarlink.ai/api/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "blackbox",
      "winterId": "blackbox",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://blackbox.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.blackbox.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 11 seeded model row(s), api root https://api.blackbox.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "bluesminds",
      "winterId": "bluesminds",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://www.bluesminds.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.bluesminds.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 22 seeded model row(s), api root https://api.bluesminds.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "byteplus",
      "winterId": "byteplus",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://console.byteplus.com/ark — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://ark.ap-southeast.bytepluses.com/api/v3. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 4 seeded model row(s), api root https://ark.ap-southeast.bytepluses.com/api/v3. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "bytez",
      "winterId": "bytez",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://bytez.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.bytez.com/models/v2/openai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.bytez.com/models/v2/openai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "cerebras",
      "winterId": "cerebras",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://inference-docs.cerebras.ai/ — Cerebras's own Inference API documentation, retrieved 2026-09-06: OpenAI-compatible base `https://api.cerebras.ai/v1` with `Authorization: Bearer ${CEREBRAS_API_KEY}`, \"mostly compatible with OpenAI's client libraries\". The base matches the API root the mapper derives from the pinned RegistryEntry, so two independent sources agree. (Round-1 finding I-3: the previous citation, `https://inference.cerebras.ai`, 301s to `chat.cerebras.ai` — a chat product, not documentation.)",
        "tier": "fetched-document"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is a FETCHED vendor document, but no live gate case has run against this endpoint"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 3 seeded model row(s), api root https://api.cerebras.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "charm-hyper",
      "winterId": "charm-hyper",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://hyper.charm.land — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://hyper.charm.land/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://hyper.charm.land/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "chutes",
      "winterId": "chutes",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://chutes.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://llm.chutes.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://llm.chutes.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "clova-studio",
      "winterId": "clova-studio",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://api.ncloud-docs.com/docs/en/ai-naver-clovastudio-summary — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://clovastudio.stream.ntruss.com/v1/openai. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 2 seeded model row(s), api root https://clovastudio.stream.ntruss.com/v1/openai. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "codestral",
      "winterId": "codestral",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://mistral.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://codestral.mistral.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://codestral.mistral.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "cohere",
      "winterId": "cohere",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://cohere.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.cohere.com/compatibility/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 6 seeded model row(s), api root https://api.cohere.com/compatibility/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "coze",
      "winterId": "coze",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://coze.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.coze.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://api.coze.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "crof",
      "winterId": "crof",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://crof.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://crof.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 15 seeded model row(s), api root https://crof.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "deepinfra",
      "winterId": "deepinfra",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://deepinfra.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.deepinfra.com/v1/openai. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.deepinfra.com/v1/openai. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "dgrid",
      "winterId": "dgrid",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://dgrid.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.dgrid.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://api.dgrid.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "dit",
      "winterId": "dit",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://dit.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.dit.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 2 seeded model row(s), api root https://api.dit.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "doubao",
      "winterId": "doubao",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://doubao.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://ark.cn-beijing.volces.com/api/v3. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 9 seeded model row(s), api root https://ark.cn-beijing.volces.com/api/v3. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "factory",
      "winterId": "factory",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://factory.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.factory.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://api.factory.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "featherless-ai",
      "winterId": "featherless-ai",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://featherless.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.featherless.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.featherless.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "fireworks",
      "winterId": "fireworks",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://fireworks.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.fireworks.ai/inference/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 10 seeded model row(s), api root https://api.fireworks.ai/inference/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "freeaiapikey",
      "winterId": "freeaiapikey",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://freeaiapikey.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.freeaiapikey.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 10 seeded model row(s), api root https://api.freeaiapikey.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "freemodel-dev",
      "winterId": "freemodel-dev",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://freemodel.dev — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.freemodel.dev/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 4 seeded model row(s), api root https://api.freemodel.dev/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "freetheai",
      "winterId": "freetheai",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://freetheai.xyz — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.freetheai.xyz/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 3 seeded model row(s), api root https://api.freetheai.xyz/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "friendliai",
      "winterId": "friendliai",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://friendli.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.friendli.ai/serverless/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.friendli.ai/serverless/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "galadriel",
      "winterId": "galadriel",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://galadriel.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.galadriel.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.galadriel.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "groq",
      "winterId": "groq",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://groq.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.groq.com/openai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 7 seeded model row(s), api root https://api.groq.com/openai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "heroku",
      "winterId": "heroku",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://www.heroku.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://us.inference.heroku.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://us.inference.heroku.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "huggingface",
      "winterId": "huggingface",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://huggingface.co — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://router.huggingface.co/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 6 seeded model row(s), api root https://router.huggingface.co/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "hyperbolic",
      "winterId": "hyperbolic",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://hyperbolic.xyz — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.hyperbolic.xyz/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 8 seeded model row(s), api root https://api.hyperbolic.xyz/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "iflytek",
      "winterId": "iflytek",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://xinghuo.xfyun.cn — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://spark-api-open.xf-yun.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 6 seeded model row(s), api root https://spark-api-open.xf-yun.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "inception",
      "winterId": "inception",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://docs.inceptionlabs.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.inceptionlabs.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://api.inceptionlabs.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "inference-net",
      "winterId": "inference-net",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://inference.net — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.inference.net/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.inference.net/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "internlm",
      "winterId": "internlm",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://internlm.intern-ai.org.cn/ — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://chat.intern-ai.org.cn/api/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 5 seeded model row(s), api root https://chat.intern-ai.org.cn/api/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "kenari",
      "winterId": "kenari",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://kenari.id — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://kenari.id/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://kenari.id/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "kie",
      "winterId": "kie",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://kie.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.kie.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 10 seeded model row(s), api root https://api.kie.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "lambda-ai",
      "winterId": "lambda-ai",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://lambda.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.lambda.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.lambda.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "liquid",
      "winterId": "liquid",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://liquid.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://inference.liquid.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://inference.liquid.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "llm7",
      "winterId": "llm7",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://llm7.io — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.llm7.io/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 4 seeded model row(s), api root https://api.llm7.io/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "longcat",
      "winterId": "longcat",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://longcat.chat/platform/docs — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.longcat.chat/openai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://api.longcat.chat/openai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "meta-llama",
      "winterId": "meta-llama",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://llama.developer.meta.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.llama.com/compat/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.llama.com/compat/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "minimax",
      "winterId": "minimax",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://platform.minimax.io/docs/api-reference/text-openai-api — MiniMax's own OpenAI-SDK reference (\"OpenAI SDK\"), retrieved 2026-09-06: `export OPENAI_BASE_URL=https://api.minimax.io/v1` with `export OPENAI_API_KEY=${YOUR_API_KEY}`, i.e. an OpenAI-compatible surface reached with the user's own key. That base is byte-identical to the API root the mapper derives from this id's pinned RegistryEntry, so two independent sources agree — which is why this row moved off the pinned-upstream tier (whole-branch review M-4: it sat there while its own `minimax-anthropic` sibling cited a fetched MiniMax document naming the same key).",
        "tier": "fetched-document"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "admission is fetched-document (the vendor's own OpenAI-SDK reference, cross-checked against the pinned RegistryEntry), but no live gate case has run against this endpoint"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 5 seeded model row(s), api root https://api.minimax.io/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them.",
      "displayNameOverride": "MiniMax (OpenAI dialect)"
    },
    {
      "upstreamId": "minimax-cn",
      "winterId": "minimax-cn",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://www.minimaxi.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.minimaxi.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 5 seeded model row(s), api root https://api.minimaxi.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "mistral",
      "winterId": "mistral",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://mistral.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.mistral.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 5 seeded model row(s), api root https://api.mistral.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "modal",
      "winterId": "modal",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://modal.com/docs — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.modal.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://api.modal.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "modelscope",
      "winterId": "modelscope",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://modelscope.cn — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api-inference.modelscope.cn/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api-inference.modelscope.cn/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "morph",
      "winterId": "morph",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://morphllm.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.morphllm.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.morphllm.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "nanogpt",
      "winterId": "nanogpt",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://nano-gpt.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://nano-gpt.com/api/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://nano-gpt.com/api/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "nlpcloud",
      "winterId": "nlpcloud",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://docs.nlpcloud.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.nlpcloud.io/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 5 seeded model row(s), api root https://api.nlpcloud.io/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "nous-research",
      "winterId": "nous-research",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://portal.nousresearch.com/help — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://inference-api.nousresearch.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 2 seeded model row(s), api root https://inference-api.nousresearch.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "novita",
      "winterId": "novita",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://novita.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.novita.ai/openai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 19 seeded model row(s), api root https://api.novita.ai/openai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "nscale",
      "winterId": "nscale",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://nscale.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://inference.api.nscale.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://inference.api.nscale.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "nube",
      "winterId": "nube",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://nube.sh — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://ai.nube.sh/api/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://ai.nube.sh/api/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "nvidia",
      "winterId": "nvidia",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://build.nvidia.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://integrate.api.nvidia.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 49 seeded model row(s), api root https://integrate.api.nvidia.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "ollama-cloud",
      "winterId": "ollama-cloud",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://ollama.com/settings/keys — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://ollama.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 12 seeded model row(s), api root https://ollama.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "openadapter",
      "winterId": "openadapter",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://openadapter.dev — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.openadapter.in/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://api.openadapter.in/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "perplexity",
      "winterId": "perplexity",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://www.perplexity.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.perplexity.ai. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 4 seeded model row(s), api root https://api.perplexity.ai. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "pioneer",
      "winterId": "pioneer",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://pioneer.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.pioneer.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 10 seeded model row(s), api root https://api.pioneer.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "plamo",
      "winterId": "plamo",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://plamo.preferredai.jp/api — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.platform.preferredai.jp/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://api.platform.preferredai.jp/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "publicai",
      "winterId": "publicai",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://publicai.co — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.publicai.co/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.publicai.co/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "qianfan",
      "winterId": "qianfan",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://cloud.baidu.com/product-s/qianfan_home — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://qianfan.baidubce.com/v2. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 3 seeded model row(s), api root https://qianfan.baidubce.com/v2. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "qiniu",
      "winterId": "qiniu",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://www.qiniu.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.qnaigc.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.qnaigc.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "qwen-cloud",
      "winterId": "qwen-cloud",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://www.qwencloud.com/ — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://dashscope-intl.aliyuncs.com/compatible-mode/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 14 seeded model row(s), api root https://dashscope-intl.aliyuncs.com/compatible-mode/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "qwen-cloud-token-plan",
      "winterId": "qwen-cloud-token-plan",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://www.qwencloud.com/pricing/token-plan — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://token-plan.ap-southeast-1.maas.aliyuncs.com/compatible-mode/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 7 seeded model row(s), api root https://token-plan.ap-southeast-1.maas.aliyuncs.com/compatible-mode/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "reka",
      "winterId": "reka",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://docs.reka.ai/chat/overview — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.reka.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 3 seeded model row(s), api root https://api.reka.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "sambanova",
      "winterId": "sambanova",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://sambanova.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.sambanova.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.sambanova.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "sarvam",
      "winterId": "sarvam",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://docs.sarvam.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.sarvam.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 2 seeded model row(s), api root https://api.sarvam.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "scaleway",
      "winterId": "scaleway",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://www.scaleway.com/en/docs/ai-data/generative-apis/ — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.scaleway.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 6 seeded model row(s), api root https://api.scaleway.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "sensenova",
      "winterId": "sensenova",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://platform.sensenova.cn — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://token.sensenova.cn/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 3 seeded model row(s), api root https://token.sensenova.cn/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "sparkdesk",
      "winterId": "sparkdesk",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://xinghuo.xfyun.cn — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://spark-api-open.xf-yun.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 4 seeded model row(s), api root https://spark-api-open.xf-yun.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "stepfun",
      "winterId": "stepfun",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://stepfun.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.stepfun.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 5 seeded model row(s), api root https://api.stepfun.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "synthetic",
      "winterId": "synthetic",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://synthetic.new — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.synthetic.new/openai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 7 seeded model row(s), api root https://api.synthetic.new/openai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "tabitoken",
      "winterId": "tabitoken",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://tabitoken.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", Anthropic Messages at https://tabitoken.com. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — Anthropic Messages dialect, 4 seeded model row(s), api root https://tabitoken.com. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "tencent",
      "winterId": "tencent",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://hunyuan.tencent.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.hunyuan.cloud.tencent.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 6 seeded model row(s), api root https://api.hunyuan.cloud.tencent.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "typhoon",
      "winterId": "typhoon",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://docs.opentyphoon.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.opentyphoon.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://api.opentyphoon.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "upstage",
      "winterId": "upstage",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://www.upstage.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.upstage.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.upstage.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "v0-vercel",
      "winterId": "v0-vercel",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://v0.dev — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.v0.dev/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.v0.dev/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "venice",
      "winterId": "venice",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://venice.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.venice.ai/api/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.venice.ai/api/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "vercel-ai-gateway",
      "winterId": "vercel-ai-gateway",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://vercel.com/docs/ai-gateway — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://ai-gateway.vercel.sh/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://ai-gateway.vercel.sh/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "volcengine",
      "winterId": "volcengine",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://www.volcengine.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://ark.cn-beijing.volces.com/api/v3. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://ark.cn-beijing.volces.com/api/v3. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "volcengine-agent-plan",
      "winterId": "volcengine-agent-plan",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://console.volcengine.com/ark/region:cn-beijing/subscription/agent-plan — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://ark.cn-beijing.volces.com/api/plan/v3. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 12 seeded model row(s), api root https://ark.cn-beijing.volces.com/api/plan/v3. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "volcengine-coding-plan",
      "winterId": "volcengine-coding-plan",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://console.volcengine.com/ark/region:cn-beijing/subscription/coding-plan — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://ark.cn-beijing.volces.com/api/coding/v3. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 9 seeded model row(s), api root https://ark.cn-beijing.volces.com/api/coding/v3. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "wafer",
      "winterId": "wafer",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://wafer.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", Anthropic Messages at https://pass.wafer.ai. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — Anthropic Messages dialect, 4 seeded model row(s), api root https://pass.wafer.ai. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "wandb",
      "winterId": "wandb",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://wandb.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.inference.wandb.ai/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 0 seeded model row(s), api root https://api.inference.wandb.ai/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "writer",
      "winterId": "writer",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://dev.writer.com — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.writer.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 2 seeded model row(s), api root https://api.writer.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "yi",
      "winterId": "yi",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://01.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://api.lingyiwanwu.com/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 1 seeded model row(s), api root https://api.lingyiwanwu.com/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "zai",
      "winterId": "zai-anthropic",
      "displayNameOverride": "Z.AI GLM Coding (Anthropic dialect)",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://docs.z.ai/devpack/tool/claude — z.ai's own GLM Coding Plan documentation for Anthropic-compatible clients, retrieved 2026-09-06: `ANTHROPIC_BASE_URL` is `\"https://api.z.ai/api/anthropic\"`, which is byte-identical to the API root the mapper derives from this id's pinned RegistryEntry (`format: \"claude\"` at `https://api.z.ai/api/anthropic/v1/messages`). Two independent sources, one value. (Round-1 finding I-4: the previous citation named `open.bigmodel.cn`, the mainland product site, and sat in the pinned-upstream tier although the lane had fetched this document.)",
        "tier": "fetched-document"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is a FETCHED vendor document, but no live gate case has run against this endpoint; the OpenAI-dialect sibling `zai` is a reviewed overlay row from the same vendor's docs"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — Anthropic Messages dialect, 7 seeded model row(s), api root https://api.z.ai/api/anthropic. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    },
    {
      "upstreamId": "zenmux",
      "winterId": "zenmux",
      "expectedCategory": "apikey",
      "pricingBasis": "token",
      "admission": {
        "basis": "api-key",
        "citation": "https://zenmux.ai — the vendor's own site as OmniRoute's product catalog records it at 5458026 (src/shared/constants/providers/apikey/*.ts, blob-pinned in extraction-manifest.json). The api-key path is attested by the pinned RegistryEntry: authType \"apikey\", OpenAI Chat Completions at https://zenmux.ai/api/v1. EVIDENCE TIER: pinned-upstream, NOT a page this lane fetched and read — see PROVENANCE.md \"Two tiers of admission citation\".",
        "tier": "pinned-upstream"
      },
      "risk": {
        "class": "review-required",
        "reasons": [
          "WS-13b §1 admission evidence is PINNED-UPSTREAM, not a fetched vendor document: the citation names the vendor site OmniRoute records, and the api-key path is attested by the pinned RegistryEntry rather than by a doc this lane read"
        ]
      },
      "$comment": "D24/R6b-4: a readable api-key entry at the pin — OpenAI Chat Completions dialect, 9 seeded model row(s), api root https://zenmux.ai/api/v1. Coding relevance is NOT asserted per row: models enter `candidate` and only the live gate (WS-13 §13) promotes any of them."
    }
  ],
  "modelOverrides": {
    "$comment": "ONE hand-reviewed per-model override map, applied by the mapper and RECORDED in the ledger as a `reviewed-normalization` — never a heuristic, and never silent. Three narrow things it can say about a single upstream model id: `id` corrects a wire id upstream spells differently from the provider's own documentation; `exclude` keeps a row out of the catalog entirely (WS-13 §4's MUST that non-`llm` rows never reach the worker-model picker — upstream has no per-model scope field, and a NAME heuristic would be worse than none because it would silently drop a future model whose id happens to match); `status` overrides the provider's `initialModelStatus` for one row. Every entry carries a `why` that ends up in the ledger, so a reviewer can diff the catalog against the pinned source and find every place they differ with the justification attached.",
    "openrouter": {
      "auto": {
        "id": "openrouter/auto",
        "why": "OpenRouter documents its auto-router as the qualified id `openrouter/auto`; the pinned upstream entry lists a bare `{ id: \"auto\" }`. Keeping upstream's spelling put `auto` on the wire from BOTH resolution paths — step 1 (the seeded row's own upstreamId) and the `allowUnlisted` pass-through, which strips a self-prefix. Correcting it here fixes both at once, and the `auto` alias keeps the bare spelling resolvable."
      }
    },
    "gemini": {
      "gemini-3.1-flash-tts-preview": {
        "exclude": true,
        "why": "A TEXT-TO-SPEECH model. WS-13 §4/§1 are a MUST: `tts` rows feed a separate subsystem and must never reach the Agent SDK worker-model picker, and the descriptor schema's `scope` is per PROVIDER — a `gemini` row cannot declare itself `tts` while its provider is `llm`. It also exposed the mapper's `outputModalities` default as a false claim: Winter stamps `[\"text\"]` on every row because upstream declares no output modality at all, which for a TTS model is simply wrong. Excluded here rather than shipped as a resolvable `candidate`."
      }
    },
    "vertex": {
      "DeepSeek-V4-Pro": {
        "status": "candidate",
        "why": "A Vertex MaaS PARTNER model. Upstream lists it under an entry whose `format` is `gemini`, so the mapper inherits the GenerateContent dialect for it — but partner models are not served over GenerateContent, and nothing has proven what they do speak here. `candidate` rather than R6-16's `experimental`, because `experimental` is a claim that a live adapter serves the row; flagged for Lane B/N."
      },
      "DeepSeek-V4-Flash": {
        "status": "candidate",
        "why": "As `DeepSeek-V4-Pro` — a Vertex MaaS partner model inheriting the Gemini dialect from a silent upstream entry."
      },
      "GLM-5.1-FP8": {
        "status": "candidate",
        "why": "As `DeepSeek-V4-Pro` — a Vertex MaaS partner model inheriting the Gemini dialect from a silent upstream entry."
      },
      "Qwen3.6-35B-A3B": {
        "status": "candidate",
        "why": "As `DeepSeek-V4-Pro` — a Vertex MaaS partner model inheriting the Gemini dialect from a silent upstream entry."
      }
    }
  },
  "categoryDispositions": {
    "$comment": "WS-13 §1's disposition table, as DATA the extractor applies. `candidate-pool` means \"eligible for the `providers` intake list above\" — never \"included\": an api-key row absent from that list is still rejected, with class `not-allowlisted`. Every other category fails closed at import time, whatever the allowlist says (the check runs BOTH ways: an allowlisted id whose upstream category is not `candidate-pool` fails the run, which is the class transition WS-13 §3 step 7 blocks).",
    "noauth": {
      "disposition": "blocked",
      "exclusionClass": "category-no-auth",
      "reason": "WS-13 §1: reject by default; individually allowlistable only after protocol and terms review"
    },
    "oauth": {
      "disposition": "blocked",
      "exclusionClass": "category-oauth",
      "reason": "WS-13 §1/§6: generic OAuth import is rejected; only provider-authorized product flows, which are Winter-owned rows, never extracted ones"
    },
    "web-cookie": {
      "disposition": "blocked",
      "exclusionClass": "category-web-cookie",
      "reason": "WS-13 §1: excluded categorically — browser-session transport"
    },
    "apikey": {
      "disposition": "candidate-pool",
      "exclusionClass": "not-allowlisted",
      "reason": "WS-13 §1: the main source pool, curated by LLM relevance and adapter quality — presence upstream is never inclusion"
    },
    "local": {
      "disposition": "winter-owned",
      "exclusionClass": "category-local-live-discovery",
      "reason": "WS-13 §12: the twelve chat/self-hosted backends are Winter-owned rows on live discovery (machine-specific inventories); image-only systems are excluded from worker models"
    },
    "search": {
      "disposition": "blocked",
      "exclusionClass": "category-search",
      "reason": "WS-13 §1: not an LLM provider"
    },
    "audio": {
      "disposition": "blocked",
      "exclusionClass": "category-audio",
      "reason": "WS-13 §1: not an Agent SDK worker-model provider"
    },
    "upstream-proxy": {
      "disposition": "blocked",
      "exclusionClass": "category-upstream-proxy",
      "reason": "WS-13 §1: no proxy-of-proxy layer"
    },
    "cloud-agent": {
      "disposition": "blocked",
      "exclusionClass": "category-cloud-agent",
      "reason": "WS-13 §1: remote agent products, not model providers"
    },
    "system": {
      "disposition": "blocked",
      "exclusionClass": "category-system",
      "reason": "WS-13 §1/§9: `auto` is routing policy, which this layer bans outright"
    }
  },
  "blocked": [
    {
      "upstreamId": "360ai",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): docs page reached (`https://ai.360.cn/docs`, HTTP 200) but it states NO OpenAI-compatible base URL, and upstream's own `authHint` says only \"Get API key at ai.360.cn\". Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "agy",
      "reason": "user ruling 2026-09-06 (WS-13b D22, §5): excluded by name from the OAuth category. No provider row, no adapter, no flag — a flag would ship code whose only purpose is to present another product's client identity, and the flag's default state does not change what the code is."
    },
    {
      "upstreamId": "aihorde",
      "reason": "the OAUTH half is DROPPED and the api-key/keyless half SHIPS as a reviewed overlay row — audit §5.1 / WS-13b §8.4 → the overlay `aihorde` row (keyless, free). This upstream id sits in the `noauth` category, whose disposition is `blocked`, so allowlisting it would be a class transition WS-13 §3 step 7 refuses outright; the overlay is the reviewed door. The row that ships carries the audit's own fetched citation, which is stronger evidence than this id's upstream entry."
    },
    {
      "upstreamId": "ainative",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): docs page reached (`https://docs.ainative.studio`, HTTP 200) but the page states no base URL; upstream's `apiHint` names `https://api.ainative.studio/api/v1`, which is ONE source, and Step 5's bar is the vendor's own document. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "aion",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`aion`, https://api.aionlabs.ai/v1) is authored from the VENDOR'S OWN documentation instead: https://www.aionlabs.ai/docs. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "amazon-q",
      "reason": "user ruling 2026-09-06 (WS-13b D22, §5): excluded by name from the OAuth category. No provider row, no adapter, no flag — a flag would ship code whose only purpose is to present another product's client identity, and the flag's default state does not change what the code is."
    },
    {
      "upstreamId": "antigravity",
      "reason": "user ruling 2026-09-06 (WS-13b D22, §5): excluded by name from the OAuth category. No provider row, no adapter, no flag — a flag would ship code whose only purpose is to present another product's client identity, and the flag's default state does not change what the code is."
    },
    {
      "upstreamId": "anyapi",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`anyapi`, https://api.anyapi.ai/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.anyapi.ai. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "arcee-ai",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`arcee-ai`, https://api.arcee.ai/api/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.arcee.ai/. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "auggie",
      "reason": "agent transport — WS-13b §8.2 (ruled EXCLUDED from Winter: \"whatever is agent transport like Claude Agent SDK, exclude it\"): stdio to an installed product. These drive an INSTALLED PRODUCT rather than serving a model endpoint, so they are not provider rows in any admissible form — and the raw `api.githubcopilot.com` path is separately excluded on identity alone (audit §3a). WS-13b §6 is retained as the record of why, so the provider layer never grows an impersonating adapter for them; nothing is owed to any phase."
    },
    {
      "upstreamId": "auriko",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): docs page reached (`https://docs.auriko.ai`, HTTP 200) and it says the API is OpenAI-compatible, but the reached page states no base URL. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "azure-ai",
      "reason": "endpoint not derivable — WS-13b §2 (round-1 finding I-1, list (b)): Azure AI Foundry has a real, documented public API, but its base URL is a PER-TENANT TEMPLATE (`https://<resource>.services.ai.azure.com/openai/v1/` or `https://<resource>.openai.azure.com/openai/v1/` — the operator's own resource name), not a fixed host. `defaultEndpoints.api` is immutable generated data (R6-11) and a template is not an endpoint, so an overlay row here would be the exact failure this lane's own endpoint rule exists to prevent — a row that validates and cannot serve a turn. Docs: https://learn.microsoft.com/azure/ai-foundry. RULING (round 1): a dedicated host-supplied-endpoint adapter shape, of the kind `azure-openai` already has, is a SPINE item for the fix wave; this id is admissible the day it exists."
    },
    {
      "upstreamId": "bailian-coding-plan",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://token-plan.ap-southeast-1.maas.aliyuncs.com/apps/anthropic/v1`, which does not end in `/v1/messages`, the path its own `format: \"claude\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "black-forest-labs",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): image generation (FLUX). `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a image service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "chat-oripe",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): HTTP 530 (Cloudflare origin unreachable) on `https://api.oriper.com` — and upstream's own `apiHint` already says to use it \"only after confirming the provider's current documentation, terms and key issuance\". Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "chatanywhere",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): docs reached (HTTP 200) but the endpoint is AMBIGUOUS BY DESIGN: the docs name `https://api.chatanywhere.tech/v1` while upstream's `apiHint` says to use `https://api.chatanywhere.org/v1` outside China. Two hosts, no single documented base — and the vendor's terms are non-commercial, which a general-purpose agent host cannot satisfy. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "cheaperinference",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"cheaperinference\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row."
    },
    {
      "upstreamId": "chenzk",
      "reason": "docs do not exist — WS-13b §1 / Step 4 (round-1 finding I-3): the `pinned-upstream` admission citation for this row named a page that is not there. Liveness sweep result: HTTP 200 but NOT A DOCUMENT: the page's entire body is the string \"New API\" — a bare gateway shell with no vendor identification, no base URL, no auth scheme, no model list and no terms (`https://chenzk.top`, read 2026-09-06). A citation is the row's entire evidence, so a citation that resolves to nothing is a row with no evidence, and WS-13b §1 does not ship one. Re-admissible through a reviewed overlay row the day a real vendor doc is found."
    },
    {
      "upstreamId": "chipotle",
      "reason": "user ruling 2026-09-06 (WS-13b D22, §5): excluded by name from the keyless/free category. Admission of the rest of that category (aihorde, uncloseai) does not reach these four."
    },
    {
      "upstreamId": "clarifai",
      "reason": "probed and NOT confirmed — WS-13b §1 (round-1 finding I-1, list (d)), probed 2026-09-06: a platform API whose chat surface and base URL were not established by the probe. Step 5's bar is a confirmed docs page AND a fixed endpoint; this id cleared neither, and Winter does not author a row from a base it cannot cite. Re-admissible the day one is found."
    },
    {
      "upstreamId": "claude",
      "reason": "user ruling 2026-09-06 (WS-13b D22, §5): excluded by name from the OAuth category. No provider row, no adapter, no flag — a flag would ship code whose only purpose is to present another product's client identity, and the flag's default state does not change what the code is."
    },
    {
      "upstreamId": "cline",
      "reason": "the OAUTH half is DROPPED and the api-key/keyless half SHIPS as a reviewed overlay row — audit §5.2 → the overlay `cline` row. This upstream id sits in the `oauth` category, whose disposition is `blocked`, so allowlisting it would be a class transition WS-13 §3 step 7 refuses outright; the overlay is the reviewed door. The row that ships carries the audit's own fetched citation, which is stronger evidence than this id's upstream entry."
    },
    {
      "upstreamId": "clinepass",
      "reason": "the OAUTH half is DROPPED and the api-key/keyless half SHIPS as a reviewed overlay row — audit §5.2 → the overlay `clinepass` row (subscription-priced). This upstream id sits in the `oauth` category, whose disposition is `blocked`, so allowlisting it would be a class transition WS-13 §3 step 7 refuses outright; the overlay is the reviewed door. The row that ships carries the audit's own fetched citation, which is stronger evidence than this id's upstream entry."
    },
    {
      "upstreamId": "cloudcode-one",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): the site was reached (HTTP 200) but there is no documentation page and it states no base URL; upstream's `apiHint` is the only source. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "cloudflare-ai",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"cloudflare-ai\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row."
    },
    {
      "upstreamId": "cloudflare-playground",
      "reason": "user ruling 2026-09-06 (WS-13b D22, §5): excluded by name from the keyless/free category. Admission of the rest of that category (aihorde, uncloseai) does not reach these four."
    },
    {
      "upstreamId": "codebuddy-cn",
      "reason": "unknown evidence — audit OQ-3 (§5.4): an API key and base URL are documented, but ONLY in the context of configuring Tencent's own CodeBuddy Code CLI, and the governing terms were not located. Missing: (a) the Coding-Plan terms of service and any clause on unofficial clients, (b) a Tencent-published statement that the key at `https://api.lkeap.cloud.tencent.com/coding/v3` may be used by non-CodeBuddy clients, plus which protocol family that endpoint speaks. WS-13b §1: a row whose evidence is `unknown` does not ship."
    },
    {
      "upstreamId": "codex-app-server",
      "reason": "agent transport — WS-13b §8.2 (ruled EXCLUDED from Winter: \"whatever is agent transport like Claude Agent SDK, exclude it\"): a websocket to the locally installed Codex app. These drive an INSTALLED PRODUCT rather than serving a model endpoint, so they are not provider rows in any admissible form — and the raw `api.githubcopilot.com` path is separately excluded on identity alone (audit §3a). WS-13b §6 is retained as the record of why, so the provider layer never grows an impersonating adapter for them; nothing is owed to any phase."
    },
    {
      "upstreamId": "comfyui",
      "reason": "Image-generation system in the `local` category — WS-13 §1 keeps it out of selectable worker models (report §4.2)."
    },
    {
      "upstreamId": "command-code",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"command-code\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row."
    },
    {
      "upstreamId": "cursor",
      "reason": "user ruling 2026-09-06 (WS-13b D22, §5): excluded by name from the OAuth category. No provider row, no adapter, no flag — a flag would ship code whose only purpose is to present another product's client identity, and the flag's default state does not change what the code is."
    },
    {
      "upstreamId": "cursor-api",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `format: undefined` is not one of the four dialects Winter speaks; `executor: \"cursor-api\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row — and `cursor` is excluded by user ruling in any case (WS-13b D22, audit §5.5)."
    },
    {
      "upstreamId": "dahl",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"openai-compatible\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row."
    },
    {
      "upstreamId": "databricks",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://adb-0000000000000000.0.azuredatabricks.net/serving-endpoints`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "datarobot",
      "reason": "probed and NOT confirmed — WS-13b §1 (round-1 finding I-1, list (d)), probed 2026-09-06: an enterprise platform whose inference host is per-deployment, not a fixed public base. Step 5's bar is a confirmed docs page AND a fixed endpoint; this id cleared neither, and Winter does not author a row from a base it cannot cite. Re-admissible the day one is found."
    },
    {
      "upstreamId": "deepai",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `format: \"custom\"` is not one of the four dialects Winter speaks. Allowlisting it would FAIL the extraction run, not produce a row — `format: \"custom\"` — a transport Winter has no adapter for."
    },
    {
      "upstreamId": "devin-cli",
      "reason": "agent transport — WS-13b §8.2 (ruled EXCLUDED from Winter: \"whatever is agent transport like Claude Agent SDK, exclude it\"): ACP over stdio to an installed product. These drive an INSTALLED PRODUCT rather than serving a model endpoint, so they are not provider rows in any admissible form — and the raw `api.githubcopilot.com` path is separately excluded on identity alone (audit §3a). WS-13b §6 is retained as the record of why, so the provider layer never grows an impersonating adapter for them; nothing is owed to any phase."
    },
    {
      "upstreamId": "devin-cli-agentic",
      "reason": "agent transport — WS-13b §8.2 (ruled EXCLUDED from Winter: \"whatever is agent transport like Claude Agent SDK, exclude it\"): ACP over stdio to an installed product. These drive an INSTALLED PRODUCT rather than serving a model endpoint, so they are not provider rows in any admissible form — and the raw `api.githubcopilot.com` path is separately excluded on identity alone (audit §3a). WS-13b §6 is retained as the record of why, so the provider layer never grows an impersonating adapter for them; nothing is owed to any phase."
    },
    {
      "upstreamId": "devin-desktop",
      "reason": "WS-13b §1 impersonation-required — audit §5.3: Devin documents no third-party auth for the desktop login (neither the API-reference overview nor the authentication page carries one; the Desktop FAQ places authentication only on app.devin.ai). Its documented REST API is a cloud-AGENT session API (`cog_` bearer), not a model endpoint, so it does not rescue the row."
    },
    {
      "upstreamId": "dify",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://api.dify.ai`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "digitalocean",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`digitalocean`, https://inference.do-ai.run/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.digitalocean.com/products/gradient-ai-platform/reference/api/serverless-inference/. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "duckduckgo-web",
      "reason": "user ruling 2026-09-06 (WS-13b D22, §5): excluded by name from the keyless/free category. Admission of the rest of that category (aihorde, uncloseai) does not reach these four."
    },
    {
      "upstreamId": "dxnt",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`dxnt`, https://www.dxnt.com/v1) is authored from the VENDOR'S OWN documentation instead: https://www.dxnt.com/docs. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "electronhub",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`electronhub`, https://api.electronhub.ai/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.electronhub.ai/api-reference/chat/completions. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "empower",
      "reason": "probed and NOT confirmed — WS-13b §1 (round-1 finding I-1, list (d)), probed 2026-09-06: no vendor documentation page was reached. Step 5's bar is a confirmed docs page AND a fixed endpoint; this id cleared neither, and Winter does not author a row from a base it cannot cite. Re-admissible the day one is found."
    },
    {
      "upstreamId": "fal-ai",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): image/video/audio generative media. `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a image service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "fastrouter",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`fastrouter`, https://api.fastrouter.ai/api/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.fastrouter.ai/. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "felo-web",
      "reason": "website scrape — WS-13b §2: kept out regardless of category. A scraped website transport (consumer web session, cookies, browser-local-storage tokens) is excluded by WS-13b §1 outright, and being in the keyless/free category does not reach it."
    },
    {
      "upstreamId": "fenayai",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): HTTP 530 (Cloudflare origin unreachable) on `https://fenayai.com`. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "free-ai",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): docs reached (HTTP 200) but the documented endpoint is NON-STANDARD: `https://api.free.ai/v1/chat/` (upstream's own `apiHint` calls it \"the nonstandard but OpenAI-shaped\" path). The API root is therefore not derivable by the rule this lane's mapper applies, and guessing one is what WS-13b §2 refuses. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "freebuff",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"freebuff\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row."
    },
    {
      "upstreamId": "freeinference",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`freeinference`, https://freeinference.org/v1) is authored from the VENDOR'S OWN documentation instead: https://freeinference.org. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "g4f-gemini",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `authType: \"optional\"` has no Winter auth kind (an \"optional\" key is neither `api-key` nor `local-none`). Allowlisting it would FAIL the extraction run, not produce a row — the `gpt4free` family proxies vendor web surfaces rather than a documented API."
    },
    {
      "upstreamId": "g4f-groq",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `authType: \"optional\"` has no Winter auth kind (an \"optional\" key is neither `api-key` nor `local-none`). Allowlisting it would FAIL the extraction run, not produce a row — the `gpt4free` family proxies vendor web surfaces rather than a documented API."
    },
    {
      "upstreamId": "g4f-nvidia",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `authType: \"optional\"` has no Winter auth kind (an \"optional\" key is neither `api-key` nor `local-none`). Allowlisting it would FAIL the extraction run, not produce a row — the `gpt4free` family proxies vendor web surfaces rather than a documented API."
    },
    {
      "upstreamId": "g4f-ollama",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `authType: \"optional\"` has no Winter auth kind (an \"optional\" key is neither `api-key` nor `local-none`). Allowlisting it would FAIL the extraction run, not produce a row — the `gpt4free` family proxies vendor web surfaces rather than a documented API."
    },
    {
      "upstreamId": "g4f-pollinations",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `authType: \"optional\"` has no Winter auth kind (an \"optional\" key is neither `api-key` nor `local-none`). Allowlisting it would FAIL the extraction run, not produce a row — the `gpt4free` family proxies vendor web surfaces rather than a documented API."
    },
    {
      "upstreamId": "getgoapi",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): docs page reached (`https://api.getgoapi.com/docs`, HTTP 200) but it is a 5 KB landing shell that states no base URL, no auth scheme and no model list. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "ghe-copilot",
      "reason": "agent transport — WS-13b §8.2 (ruled EXCLUDED from Winter: \"whatever is agent transport like Claude Agent SDK, exclude it\"): as `github`; verified for GitHub Enterprise Cloud, and the same JSON-RPC subprocess transport. These drive an INSTALLED PRODUCT rather than serving a model endpoint, so they are not provider rows in any admissible form — and the raw `api.githubcopilot.com` path is separately excluded on identity alone (audit §3a). WS-13b §6 is retained as the record of why, so the provider layer never grows an impersonating adapter for them; nothing is owed to any phase."
    },
    {
      "upstreamId": "gigachat",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://gigachat.devices.sberbank.ru/api/v1`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "github",
      "reason": "agent transport — WS-13b §8.2 (ruled EXCLUDED from Winter: \"whatever is agent transport like Claude Agent SDK, exclude it\"): GitHub's documented third-party route is the Copilot SDK / Copilot CLI, whose transport SPAWNS a local Copilot CLI in server mode over JSON-RPC. These drive an INSTALLED PRODUCT rather than serving a model endpoint, so they are not provider rows in any admissible form — and the raw `api.githubcopilot.com` path is separately excluded on identity alone (audit §3a). WS-13b §6 is retained as the record of why, so the provider layer never grows an impersonating adapter for them; nothing is owed to any phase."
    },
    {
      "upstreamId": "gitlab",
      "reason": "probed and NOT confirmed — round-1 finding I-1, list (d): this id COLLAPSES INTO the deferred `gitlab-duo` entry above, which carries the full ruling. GitLab's AI surface is code-completion REST plus a GraphQL `aiAction` chat mutation — a custom transport, not an existing Winter dialect — and is marked \"Status: Experiment\" upstream. Nothing is owed to any phase (R6b-8)."
    },
    {
      "upstreamId": "gitlab-duo",
      "reason": "deferred custom transport — WS-13b §4 (R6b-8): documented-third-party under prong 1 (the `ai_features` scope is documented for BOTH a self-registered OAuth application and a PAT), but the surface is code-completion REST + a GraphQL `aiAction` chat mutation — a CUSTOM transport, not an existing Winter dialect, and marked \"Status: Experiment\" upstream. Admissible under §1 when a transport slice exists; no row in P6.5, and nothing is owed to any phase."
    },
    {
      "upstreamId": "gitlawb",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://opengateway.gitlawb.com/v1/xiaomi-mimo`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "gitlawb-gmi",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://opengateway.gitlawb.com/v1/gmi-cloud`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "glm",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"glm\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row — folded into the `zai`/`zai-anthropic` sibling pair (R6b-5): both z.ai GLM Coding dialects ship as reviewed rows carrying the vendor's own documented endpoints."
    },
    {
      "upstreamId": "glm-cn",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"glm\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row — folded into the `zai`/`zai-anthropic` sibling pair (R6b-5): both z.ai GLM Coding dialects ship as reviewed rows carrying the vendor's own documented endpoints."
    },
    {
      "upstreamId": "glmt",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"glm\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row — folded into the `zai`/`zai-anthropic` sibling pair (R6b-5): both z.ai GLM Coding dialects ship as reviewed rows carrying the vendor's own documented endpoints."
    },
    {
      "upstreamId": "grok-cli",
      "reason": "WS-13b §1 impersonation-required — audit §5.3/§2.5: as the ecosystem uses this name the id REUSES the official Grok CLI's stored credentials (`~/.grok/auth.json`) — a session the user granted to Grok Build on a consent screen Winter never showed, with no login and no identity of ours in the exchange. That fails even the codex shape (Winter performs its OWN codex login; it does not read `~/.codex/auth.json`). Winter's own login is the `xai-oauth` row."
    },
    {
      "upstreamId": "haiper",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://api.haiper.ai/v1`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "hcnsec",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): TRAP, named in the round-1 findings: the pinned entry declares `format: \"claude\"` with a `/v1/chat/completions` base — the entry CONTRADICTS ITS OWN DIALECT, so neither value can be trusted. The vendor's doc is the tie-breaker and there is none: `https://api.hcnsec.cn/docs` is a 4.5 KB page stating no base URL. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "helixmind",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): TRAP, named in the round-1 findings: the pinned entry declares `format: \"claude\"` with a `/v1/chat/completions` base, contradicting its own dialect. HTTP 403 on `https://helixmind.online`, so no vendor document could break the tie. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "helyxai",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`helyxai`, https://helyxai.space/v1) is authored from the VENDOR'S OWN documentation instead: https://helyxai.space. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "ideogram",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://api.ideogram.ai`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "jina-ai",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): text embeddings and rerankers. `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a embedding service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "jina-reader",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): URL-to-text extraction (a reader service). `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a tooling service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "kilo-gateway",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `authType: \"optional\"` has no Winter auth kind (an \"optional\" key is neither `api-key` nor `local-none`). Allowlisting it would FAIL the extraction run, not produce a row."
    },
    {
      "upstreamId": "kilocode",
      "reason": "the OAUTH half is DROPPED and the api-key/keyless half SHIPS as a reviewed overlay row — audit §5.2 → the overlay `kilocode` row. This upstream id sits in the `oauth` category, whose disposition is `blocked`, so allowlisting it would be a class transition WS-13 §3 step 7 refuses outright; the overlay is the reviewed door. The row that ships carries the audit's own fetched citation, which is stronger evidence than this id's upstream entry."
    },
    {
      "upstreamId": "kimi",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"moonshot\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row — a DUPLICATE of `moonshot` at this pin (same executor, same `api.moonshot.ai` base, same four models); `moonshot` is the row that ships."
    },
    {
      "upstreamId": "kimi-coding",
      "reason": "the OAUTH half is DROPPED and the api-key/keyless half SHIPS as a reviewed overlay row — audit §5.2/§4 → the overlay `kimi-coding` row (subscription-priced, Anthropic dialect). This upstream id sits in the `oauth` category, whose disposition is `blocked`, so allowlisting it would be a class transition WS-13 §3 step 7 refuses outright; the overlay is the reviewed door. The row that ships carries the audit's own fetched citation, which is stronger evidence than this id's upstream entry."
    },
    {
      "upstreamId": "kimi-coding-apikey",
      "reason": "endpoint not derivable — WS-13b §2: upstream states NO `baseUrl` at all, which does not end in `/v1/messages`, the path its own `format: \"claude\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "kiro",
      "reason": "user ruling 2026-09-06 (WS-13b D22, §5): excluded by name from the OAuth category. No provider row, no adapter, no flag — a flag would ship code whose only purpose is to present another product's client identity, and the flag's default state does not change what the code is."
    },
    {
      "upstreamId": "laozhang",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): docs reached (`https://docs.laozhang.ai`, HTTP 200) but the reached page is regional-availability prose and states no base URL. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "leonardo",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://cloud.leonardo.ai/api/rest/v1`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "literouter",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`literouter`, https://api.literouter.com/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.literouter.com. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "llamagate",
      "reason": "docs do not exist — WS-13b §1 / Step 4 (round-1 finding I-3): the `pinned-upstream` admission citation for this row named a page that is not there. Liveness sweep result: NXDOMAIN on 2026-09-06 (`https://llamagate.ai` does not resolve). A citation is the row's entire evidence, so a citation that resolves to nothing is a row with no evidence, and WS-13b §1 does not ship one. Re-admissible through a reviewed overlay row the day a real vendor doc is found."
    },
    {
      "upstreamId": "llm-kiwi",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`llm-kiwi`, https://api.llm.kiwi/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.llm.kiwi/. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "llmgateway",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`llmgateway`, https://api.llmgateway.io/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.llmgateway.io/. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "logfare",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`logfare`, https://logfare.ai/v1) is authored from the VENDOR'S OWN documentation instead: https://logfare.ai/docs. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "magnific",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): image upscaling and enhancement. `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a image service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "maritalk",
      "reason": "endpoint not derivable — WS-13b §2: upstream states NO `baseUrl` at all, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "meganova-ai",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`meganova-ai`, https://api.meganova.ai/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.meganova.ai. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "mixedbread",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): text embeddings and rerankers. `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a embedding service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "mixlayer",
      "reason": "probed and NOT confirmed — WS-13b §1 (round-1 finding I-1, list (d)), probed 2026-09-06: no vendor documentation page with a fixed OpenAI-compatible base was reached. Step 5's bar is a confirmed docs page AND a fixed endpoint; this id cleared neither, and Winter does not author a row from a base it cannot cite. Re-admissible the day one is found."
    },
    {
      "upstreamId": "mnn-ai",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`mnn-ai`, https://api.mnnai.ru/v1) is authored from the VENDOR'S OWN documentation instead: https://mnnai.ru/docs. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "monsterapi",
      "reason": "docs do not exist — WS-13b §1 / Step 4 (round-1 finding I-3): the `pinned-upstream` admission citation for this row named a page that is not there. Liveness sweep result: NXDOMAIN on 2026-09-06 (`https://monsterapi.ai` does not resolve). A citation is the row's entire evidence, so a citation that resolves to nothing is a row with no evidence, and WS-13b §1 does not ship one. Re-admissible through a reviewed overlay row the day a real vendor doc is found."
    },
    {
      "upstreamId": "moonshot",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"moonshot\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row — ships instead as the reviewed overlay `moonshot` row (token-priced platform API) beside `kimi-coding` (subscription), per audit §4."
    },
    {
      "upstreamId": "muse-code",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): TRAP, named in the round-1 findings: NO VENDOR DOC EXISTS for it. Its recorded `website` is a GitHub repository URL and the probe resolved to `docs.github.com`. Upstream also routes it through the Responses API with a proprietary `/v1/muse-code/models` catalogue, so even the dialect is not the shipped one. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "naga-ac",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `authType: \"optional\"` has no Winter auth kind (an \"optional\" key is neither `api-key` nor `local-none`). Allowlisting it would FAIL the extraction run, not produce a row."
    },
    {
      "upstreamId": "naga-ai",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`naga-ai`, https://api.naga.ac/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.naga.ac/api-reference/overview. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "nara",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): the site was reached (`https://bynara.id`, HTTP 200) but it is an education-product page with no API documentation; upstream's own `authHint` says the key comes \"via NaraRouter's Telegram channel\", which is not a documented third-party path (WS-13b §1). Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "navy",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): HTTP 403 on `https://api.navy` — no vendor document could be read. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "nebius",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`nebius`, https://api.tokenfactory.nebius.com/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.tokenfactory.nebius.com/. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "nomic",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): text embeddings. `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a embedding service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "oci",
      "reason": "endpoint not derivable — WS-13b §2 (round-1 finding I-1, list (b)): OCI Generative AI has a real, documented public API, but its base URL is a PER-TENANT TEMPLATE (`https://inference.generativeai.<region>.oci.oraclecloud.com/openai/v1/` — the operator's own region), not a fixed host. `defaultEndpoints.api` is immutable generated data (R6-11) and a template is not an endpoint, so an overlay row here would be the exact failure this lane's own endpoint rule exists to prevent — a row that validates and cannot serve a turn. Docs: https://www.oracle.com/artificial-intelligence/generative-ai. RULING (round 1): a dedicated host-supplied-endpoint adapter shape, of the kind `azure-openai` already has, is a SPINE item for the fix wave; this id is admissible the day it exists."
    },
    {
      "upstreamId": "ofoxai",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`ofoxai`, https://api.ofox.ai/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.ofox.ai. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "opencode",
      "reason": "the OAUTH half is DROPPED and the api-key/keyless half SHIPS as a reviewed overlay row — audit §5.2 → the overlay `opencode` row. This upstream id sits in the `noauth` category, whose disposition is `blocked`, so allowlisting it would be a class transition WS-13 §3 step 7 refuses outright; the overlay is the reviewed door. The row that ships carries the audit's own fetched citation, which is stronger evidence than this id's upstream entry."
    },
    {
      "upstreamId": "opencode-go",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"opencode\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row — folded into the reviewed overlay `opencode` row (OpenCode Zen, audit §5.2)."
    },
    {
      "upstreamId": "opencode-zen",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"opencode\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row — folded into the reviewed overlay `opencode` row (OpenCode Zen, audit §5.2)."
    },
    {
      "upstreamId": "openference",
      "reason": "the OAUTH half is DROPPED and the api-key/keyless half SHIPS as a reviewed overlay row — audit §5.2 → the overlay `openference` row. This upstream id sits in the `oauth` category, whose disposition is `blocked`, so allowlisting it would be a class transition WS-13 §3 step 7 refuses outright; the overlay is the reviewed door. The row that ships carries the audit's own fetched citation, which is stronger evidence than this id's upstream entry."
    },
    {
      "upstreamId": "openference-api",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): TRAP, named in the round-1 findings: NOT A SEPARATE PROVIDER. Its docs (`https://docs.openference.com`) are the SAME documents that admit the shipped overlay `openference` row, on the same base `https://api.openference.com/v1`. Admitting it would be one vendor wearing two ids, and a `set_model` between them would look like a switch while changing nothing. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "openvecta",
      "reason": "probed and NOT confirmed — WS-13b §1 (round-1 finding I-1, list (d)), probed 2026-09-06: no vendor documentation page with a fixed OpenAI-compatible base was reached. Step 5's bar is a confirmed docs page AND a fixed endpoint; this id cleared neither, and Winter does not author a row from a base it cannot cite. Re-admissible the day one is found."
    },
    {
      "upstreamId": "orcarouter",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://api.orcarouter.ai/v1`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "ovhcloud",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `authType: \"optional\"` has no Winter auth kind (an \"optional\" key is neither `api-key` nor `local-none`). Allowlisting it would FAIL the extraction run, not produce a row."
    },
    {
      "upstreamId": "piapi",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): docs reached (`https://piapi.ai/docs`, HTTP 200) and they identify it as \"AI Model APIs for Image, …\" — a generative-MEDIA aggregator. Out of scope on the same ground as list (c) (WS-13 §1/§4: this catalog carries `llm` rows only), and the probe is recorded so the scope call is checkable rather than a name heuristic. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "poe",
      "reason": "endpoint not derivable — WS-13b §2: upstream states NO `baseUrl` at all, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "poixe-ai",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): docs reached (`https://docs.poixe.com`, HTTP 200) but the reached page is an index; the base URL lives behind a further API-reference page this probe did not confirm. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "pollinations",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `authType: \"optional\"` has no Winter auth kind (an \"optional\" key is neither `api-key` nor `local-none`); `executor: \"pollinations\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row."
    },
    {
      "upstreamId": "poolside",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`poolside`, https://inference.poolside.ai/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.poolside.ai/api/overview. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "predibase",
      "reason": "docs do not exist — WS-13b §1 / Step 4 (round-1 finding I-3): the `pinned-upstream` admission citation for this row named a page that is not there. Liveness sweep result: HTTP 301 to `https://www.rubrik.com/products/rubrik-agent-cloud` on 2026-09-06 — the cited page is permanently redirected to a DIFFERENT COMPANY's product page (an acquisition), so it documents nothing about this provider. A citation is the row's entire evidence, so a citation that resolves to nothing is a row with no evidence, and WS-13b §1 does not ship one. Re-admissible through a reviewed overlay row the day a real vendor doc is found."
    },
    {
      "upstreamId": "qoder",
      "reason": "recorded user ruling (audit §5.5, WS-13b §4): ALLOWED, and owned by lane O as a Winter-authored OAuth flow — not an extracted row. Its upstream category is `oauth` (blocked), and WS-13 §6 is explicit that Winter's OAuth providers are Winter-owned rows, never extracted ones. Audit OQ-5 names the one document that pass could not find: a Qoder-documented model-INFERENCE endpoint for third-party clients."
    },
    {
      "upstreamId": "raycast",
      "reason": "WS-13b §1 impersonation-required — audit §5.3: the Raycast AI API is callable ONLY from inside a Raycast extension running in the Raycast app, gated in-process on `environment.canAccess(AI)`. No HTTP surface and no external client access are documented, and the ToS forbids reverse engineering and scraping."
    },
    {
      "upstreamId": "recraft",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): image and vector generation. `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a image service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "regolo",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://api.regolo.ai`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "requesty",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`requesty`, https://router.requesty.ai/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.requesty.ai/quickstart. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "routeway",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`routeway`, https://api.routeway.ai/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.routeway.ai/. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "runwayml",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): video generation. `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a image service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "sap",
      "reason": "probed and NOT confirmed — WS-13b §1 (round-1 finding I-1, list (d)), probed 2026-09-06: SAP Generative AI Hub is reached through a customer's own AI Core deployment, not a fixed public host. Step 5's bar is a confirmed docs page AND a fixed endpoint; this id cleared neither, and Winter does not author a row from a base it cannot cite. Re-admissible the day one is found."
    },
    {
      "upstreamId": "sdwebui",
      "reason": "Image-generation system in the `local` category — WS-13 §1 keeps it out of selectable worker models (report §4.2)."
    },
    {
      "upstreamId": "sealion",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`sealion`, https://api.sea-lion.ai/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.sea-lion.ai/guides/inferencing/api/. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "segmind",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): image/video generative media. `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a image service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "siliconflow",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`siliconflow`, https://api.siliconflow.com/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.siliconflow.com/en/api-reference/chat-completions/chat-completions. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "snowflake",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://{account}.snowflakecomputing.com/api/v2`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "speka",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`speka`, https://speka.me/v1) is authored from the VENDOR'S OWN documentation instead: https://speka.me/docs. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "stability-ai",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): image generation (Stable Diffusion). `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a image service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "sumopod",
      "reason": "docs do not exist — WS-13b §1 / Step 4 (round-1 finding I-3): the `pinned-upstream` admission citation for this row named a page that is not there. Liveness sweep result: HTTP 404 on 2026-09-06 (`https://ai.sumopod.com`). A citation is the row's entire evidence, so a citation that resolves to nothing is a row with no evidence, and WS-13b §1 does not ship one. Re-admissible through a reviewed overlay row the day a real vendor doc is found."
    },
    {
      "upstreamId": "suno",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `authType: \"cookie\"` has no Winter auth kind (an \"optional\" key is neither `api-key` nor `local-none`). Allowlisting it would FAIL the extraction run, not produce a row — a music generator, and `authType: \"cookie\"` is a browser-session transport WS-13b §1 excludes outright."
    },
    {
      "upstreamId": "thebai",
      "reason": "probed and NOT confirmed — WS-13b §1 (round-1 finding I-1, list (d)), probed 2026-09-06: no vendor documentation page was reached. Step 5's bar is a confirmed docs page AND a fixed endpoint; this id cleared neither, and Winter does not author a row from a base it cannot cite. Re-admissible the day one is found."
    },
    {
      "upstreamId": "theoldllm",
      "reason": "website scrape — WS-13b §2: kept out regardless of category. A scraped website transport (consumer web session, cookies, browser-local-storage tokens) is excluded by WS-13b §1 outright, and being in the keyless/free category does not reach it."
    },
    {
      "upstreamId": "tinyfish",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): web data extraction / browsing automation. `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a tooling service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "together",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`together`, https://api.together.ai/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.together.ai/docs/chat-overview. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "token-kiosk",
      "reason": "docs do not exist — WS-13b §1 / Step 4 (round-1 finding I-3): the `pinned-upstream` admission citation for this row named a page that is not there. Liveness sweep result: HTTP 404 on 2026-09-06 (`https://agent-router.gaib.ai`). A citation is the row's entire evidence, so a citation that resolves to nothing is a row with no evidence, and WS-13b §1 does not ship one. Re-admissible through a reviewed overlay row the day a real vendor doc is found."
    },
    {
      "upstreamId": "tokenreply",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): docs page reached (`https://www.tokenreply.com/docs`, HTTP 200) but it is an 8 KB shell stating no base URL. Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "tokenrouter",
      "reason": "docs do not exist — WS-13b §1 / Step 4 (round-1 finding I-3): the `pinned-upstream` admission citation for this row named a page that is not there. Liveness sweep result: NXDOMAIN on 2026-09-06 (`https://tokenrouter.com` does not resolve). A citation is the row's entire evidence, so a citation that resolves to nothing is a row with no evidence, and WS-13b §1 does not ship one. Re-admissible through a reviewed overlay row the day a real vendor doc is found."
    },
    {
      "upstreamId": "topaz",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): image and video enhancement. `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a image service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "trae",
      "reason": "user ruling 2026-09-06 (WS-13b D22, §5): excluded by name from the OAuth category. No provider row, no adapter, no flag — a flag would ship code whose only purpose is to present another product's client identity, and the flag's default state does not change what the code is."
    },
    {
      "upstreamId": "udio",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://www.udio.com/api/generate-proxy`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "uncloseai",
      "reason": "the OAUTH half is DROPPED and the api-key/keyless half SHIPS as a reviewed overlay row — audit §5.1 / WS-13b §8.4 → the overlay `uncloseai` row (keyless, free). This upstream id sits in the `noauth` category, whose disposition is `blocked`, so allowlisting it would be a class transition WS-13 §3 step 7 refuses outright; the overlay is the reviewed door. The row that ships carries the audit's own fetched citation, which is stronger evidence than this id's upstream entry."
    },
    {
      "upstreamId": "unorouter",
      "reason": "probed 2026-09-06 and NOT admitted — WS-13b §1 / Step 5 (round-1 finding I-1, list (e)): docs reached (`https://unorouter.ai/docs`, HTTP 200) but the platform guide states no OpenAI-compatible base URL; upstream's own `apiHint` names none either, saying only \"Create an API key at https://unorouter.ai\". Step 5's bar is a confirmed vendor documentation page AND a fixed endpoint; hand-transcribing the helper call's arguments out of the pinned tree is the extraction the literal extractor refuses, so it is not the substitute. Re-admissible through a reviewed overlay row the day the document is found."
    },
    {
      "upstreamId": "veoaifree-web",
      "reason": "website scrape — WS-13b §2: kept out regardless of category. A scraped website transport (consumer web session, cookies, browser-local-storage tokens) is excluded by WS-13b §1 outright, and being in the keyless/free category does not reach it."
    },
    {
      "upstreamId": "vertex-partner",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://us-central1-aiplatform.googleapis.com/v1/projects`, which does not end in `/v1beta/models`, the path its own `format: \"gemini\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "void-ai",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`void-ai`, https://api.voidai.app/v1) is authored from the VENDOR'S OWN documentation instead: https://docs.voidai.app/. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "voyage-ai",
      "reason": "out of scope — WS-13 §1/§4, D24 (round-1 finding I-1, list (c)): text embeddings and rerankers. `WinterProviderDescriptor.scope` is per PROVIDER and this catalog carries `llm` rows only; a embedding service has no chat surface to bind and must never reach the Agent SDK worker-model picker. Re-filed off \"unknown evidence\" in round 1: this id is not held pending a document — MORE evidence would not admit it, which is what makes it a different exclusion from the ones that are."
    },
    {
      "upstreamId": "watsonx",
      "reason": "probed and NOT confirmed — WS-13b §1 (round-1 finding I-1, list (d)), probed 2026-09-06: IBM documents a watsonx.ai model gateway, but the base is a per-region/self-managed template (`https://<region>.ml.cloud.ibm.com/ml/gateway/v1/`) rather than a fixed host — the same class as list (b). Step 5's bar is a confirmed docs page AND a fixed endpoint; this id cleared neither, and Winter does not author a row from a base it cannot cite. Re-admissible the day one is found."
    },
    {
      "upstreamId": "x5lab",
      "reason": "docs do not exist — WS-13b §1 / Step 4 (round-1 finding I-3): the `pinned-upstream` admission citation for this row named a page that is not there. Liveness sweep result: HTTP 530 on 2026-09-06, on a HEAD and on a retried GET (`https://x5lab.dev` — Cloudflare origin unreachable). A citation is the row's entire evidence, so a citation that resolves to nothing is a row with no evidence, and WS-13b §1 does not ship one. Re-admissible through a reviewed overlay row the day a real vendor doc is found."
    },
    {
      "upstreamId": "xai",
      "reason": "unrepresentable upstream vocabulary — WS-13 §13 / WS-13b §2: `executor: \"xai\"` is a bespoke OmniRoute execution path Winter does not represent — WS-13 §1's exclusion classes ARE the executors the mapper's closed set omits, and an unknown executor fails the run rather than defaulting. Allowlisting it would FAIL the extraction run, not produce a row. The api-key half SHIPS as the reviewed overlay `xai` row (`https://api.x.ai/v1`, token-priced, authored from docs.x.ai/docs/overview), and the subscription half is lane O's Winter-authored `xai-oauth` flow — this id is kept out of the EXTRACTION, not out of the catalog."
    },
    {
      "upstreamId": "xai-oauth",
      "reason": "owned by lane O as a Winter-authored OAuth flow (WS-13b §4, R6b-6) — never an extracted row: its upstream entry carries an OAuth client id under `resolvePublicCred(...)`, which is credential material the extractor refuses, and WS-13 §6 makes Winter's OAuth providers Winter-owned rows in any case. Admitted under prong 2 (audit §2.5, the codex shape) with a REVERSION CONDITION and a hot-reloadable setting."
    },
    {
      "upstreamId": "xiaomi-mimo",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://api.xiaomimimo.com/v1`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "xiaomi-mimo-token-plan",
      "reason": "endpoint not derivable — WS-13b §2: upstream states `https://token-plan-sgp.xiaomimimo.com/v1`, which does not end in `/chat/completions`, the path its own `format: \"openai\"` names and the path Winter's adapter for that family APPENDS to `connection.baseUrl`. The API root is therefore not derivable from the pinned tree — upstream's executor is outside the import boundary, so what it does with this URL is unknowable here — and Winter refuses to guess an endpoint rather than ship a row that would 404 or reach the wrong host. Admissible later through a reviewed overlay row authored from the vendor's own docs."
    },
    {
      "upstreamId": "yolo-auto",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`yolo-auto`, https://yolo-auto.com/v1) is authored from the VENDOR'S OWN documentation instead: https://yolo-auto.com/docs. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "zcode",
      "reason": "user ruling 2026-09-06 (WS-13b D22, §5): excluded by name from the keyless/free category. Admission of the rest of that category (aihorde, uncloseai) does not reach these four."
    },
    {
      "upstreamId": "zed",
      "reason": "user ruling 2026-09-06 (WS-13b D22, §5): excluded by name from the OAuth category. No provider row, no adapter, no flag — a flag would ship code whose only purpose is to present another product's client identity, and the flag's default state does not change what the code is."
    },
    {
      "upstreamId": "zed-hosted",
      "reason": "WS-13b §1 impersonation-required — audit §5.3, ruled EXCLUDED at WS-13b §8.1: Zed documents no non-Zed client use at all (no base URL, no key, no OAuth — the ABSENCE is the finding), and its ToS §2.2 restricts access to \"the mechanisms designated by Zed\" while §2.4 forbids access to non-public APIs. Not even disabled-behind-a-flag: a flag would ship code whose only purpose is to present a Zed client identity."
    },
    {
      "upstreamId": "zerolimitai",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`zerolimitai`, https://www.zerolimitai.com/api/v1) is authored from the VENDOR'S OWN documentation instead: https://www.zerolimitai.com. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    },
    {
      "upstreamId": "zylo-api",
      "reason": "the EXTRACTION is refused and the provider SHIPS as a reviewed overlay row — WS-13b §2 / round-1 finding I-1. Its upstream entry is built by a helper CALL the literal extractor will not evaluate, so the pinned tree yields no endpoint, auth or executor for it and allowlisting it would produce a row with no identity. The row that ships (`zylo-api`, https://api.zyloai.net/v1) is authored from the VENDOR'S OWN documentation instead: https://zyloai.net/docs. Named here so this id carries a ruling of its own rather than falling back to the generic `not-allowlisted` class — the same cross-reference `aihorde` and `cline` carry, and the same one pattern."
    }
  ],
  "notes": [
    "`codex-oauth` is NOT extracted from upstream — it is a Winter-owned provider carried over per D11 with originator \"winter\" (WS-01 §3), and its descriptor's `upstream.project` is \"winter\".",
    "The twelve local OpenAI-compatible ids (WS-13 §12) are likewise Winter-owned: their inventories are machine-specific and always come from live discovery, so there is no upstream row to extract.",
    "Promoting a provider from `blocked`/`review-required` to `approved`, or adding an id to `providers`, is the reviewed allowlist change WS-13 §13 requires before anything can reach `supported`. The extractor has no other door.",
    "`blocked` names INDIVIDUAL upstream ids only. It previously carried a `\"*\"` sentinel restating WS-13 §5/§6's categorical exclusions (browser sessions, private APIs, consumer subscriptions); the mapper filtered that row out, so it was documentation wearing the shape of a rule. Those exclusions are ENFORCED by `categoryDispositions` above, which fails the run both ways — an allowlisted id whose category is not the api-key candidate pool aborts extraction — so the sentinel is dropped rather than left looking load-bearing."
  ]
}
