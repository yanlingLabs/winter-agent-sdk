{
  "$comment": "Extractor INPUT contract (WS-13 §3/§13; OmniRoute report §11.2). Hand-maintained and reviewed, in two halves. (1) `paths` is the VERSIONED path allowlist the sparse checkout materializes — the extractor refuses to materialize anything outside it and never widens the boundary on its own. (2) `providers` is the reviewed provider intake list: a row's `risk` is what the extractor stamps onto the generated WinterProviderDescriptor, and promoting one to \"approved\" is a deliberate, reviewed edit. No upstream source is vendored under this directory beyond the upstream LICENSE/NOTICE copies the extraction manifest records — see README.md.",
  "allowlistVersion": 2,
  "upstreamProject": "OmniRoute",

  "importBoundary": {
    "$comment": "WS-13 §3 step 2 says an import outside the allowlist \"fails the job for review; the boundary never auto-widens\". Taken as a rule about WIDENING, not about aborting: the literal extractor never follows an import at all, so a value that would have to come from an unmaterialized module is DROPPED with a recorded rejection (`unresolved-reference`), and the offending specifier is listed in the ledger's `outOfAllowlistImports` so drift stays visible. Aborting instead would make the pipeline unrunnable by construction — `open-sse/config/providers/shared.ts` imports a dozen modules outside any reviewable provider-data boundary. The boundary keeps its teeth through `failOnUnresolvedFields`: an allowlisted provider whose IDENTITY-critical field cannot be resolved from materialized files fails the run for review.",
    "resolveIdentifiersWithin": "materialized-allowlisted-files-only",
    "failOnUnresolvedFields": ["id", "format", "executor", "authType"]
  },

  "paths": [
    { "pattern": "/LICENSE", "role": "notice", "why": "OmniRoute root MIT licence — copied into third_party/ and registered in the extraction manifest (report §12)." },
    { "pattern": "/THIRD_PARTY_NOTICES.md", "role": "notice", "why": "Upstream's own third-party notices — copied beside the licence so a reader of Winter's NOTICE can reach upstream's provenance record." },
    { "pattern": "/package.json", "role": "claim", "why": "The upstream package version at the pin. Read for the provenance record only; no descriptor field is derived from it." },
    { "pattern": "/README.md", "role": "claim", "why": "One of the two upstream provider-COUNT claim sources. Read only to compute the denominator discrepancy WS-13 §3 step 5 requires the extractor to report; never a descriptor input." },
    { "pattern": "/docs/reference/PROVIDER_REFERENCE.md", "role": "claim", "why": "The generated provider reference — the second count claim source, and the per-category denominator. Report §5 warns its header is not a release authority, so it is read as a CLAIM to be checked against the enumerated tree, never as truth." },
    { "pattern": "/open-sse/config/providerPluginManifest.ts", "role": "extract", "why": "The upstream-supported JSON-safe projection boundary (report §5) — read for its own reviewed field list." },
    { "pattern": "/open-sse/config/providerPluginManifestRegistry.ts", "role": "extract", "why": "Companion to the manifest projection." },
    { "pattern": "/open-sse/config/providerPluginManifestUrl.ts", "role": "extract", "why": "Companion to the manifest projection." },
    { "pattern": "/open-sse/config/providers/**", "role": "extract", "why": "The backend registry (report §3.2): the execution-shaped provider entries and their static model rows. This is the main descriptor input." },
    { "pattern": "/src/shared/constants/providers.ts", "role": "extract", "why": "The product-catalog barrel (report §3.1) — category membership and display metadata." },
    { "pattern": "/src/shared/constants/providers/**", "role": "extract", "why": "The per-category product-catalog literals. These carry the category each upstream id belongs to, which is what WS-13 §1's disposition table keys on." },
    { "pattern": "/src/lib/providerModels/modelDiscovery.ts", "role": "extract", "why": "The live-discovery normalizer (report §3.3) — read for the discovery response shapes Winter's own bounded discovery must accept." },
    { "pattern": "/src/lib/db/migrations/*retire*.sql", "role": "extract", "why": "Retirement migrations for GPL-derived and provenance-hold integrations (report §12). ZERO MATCHES AT THIS PIN — see PROVENANCE.md; the pattern stays so a future bump picks them up." },
    { "pattern": "/src/lib/db/migrations/*provenance*.sql", "role": "extract", "why": "Provenance-hold migrations. Zero matches at this pin, kept for the same reason." }
  ],

  "providers": [
    { "upstreamId": "openai", "winterId": "openai", "expectedCategory": "apikey", "risk": { "class": "approved", "reasons": [] } },
    { "upstreamId": "anthropic", "winterId": "anthropic", "expectedCategory": "apikey", "risk": { "class": "approved", "reasons": [] } },
    { "upstreamId": "gemini", "winterId": "google", "expectedCategory": "apikey", "risk": { "class": "approved", "reasons": [] },
      "$comment": "The upstream id is `gemini`, not `google` — allowlistVersion 1 named `google` on both sides, which matched no upstream row at v3.8.50 and would have silently produced no google extraction at all. `winterId` stays `google` because WS-13 §12 and every seeded row use it." },
    { "upstreamId": "openrouter", "winterId": "openrouter", "expectedCategory": "apikey", "risk": { "class": "review-required", "reasons": ["gateway: capability parity is model-by-model (WS-13 §14.4); per-request provider/data-policy controls must be preserved"] } },
    { "upstreamId": "deepseek", "winterId": "deepseek", "expectedCategory": "apikey", "risk": { "class": "review-required", "reasons": ["reasoning continuity is tool-loop fragile (WS-13 §8.2: missing reasoning state mid-tool-loop is a hard error)"] } },
    { "upstreamId": "azure-openai", "winterId": "azure-openai", "expectedCategory": "apikey", "risk": { "class": "review-required", "reasons": ["native cloud: deployment/api-version identity and Entra auth need their own proof (WS-13 §5)"] },
      "$comment": "PRODUCT-CATALOG ONLY at this pin: `azure-openai` has a row in src/shared/constants/providers/apikey/enterprise-cloud.ts but NO backend RegistryEntry, so upstream contributes its identity and nothing executable. Its endpoints and auth are Winter-authored in the overlay; the rejection ledger records the absence." },
    { "upstreamId": "bedrock", "winterId": "bedrock", "expectedCategory": "apikey", "risk": { "class": "review-required", "reasons": ["native cloud: SigV4 signing + event-stream decoding need their own proof (WS-13 §5)"] } },
    { "upstreamId": "vertex", "winterId": "vertex", "expectedCategory": "apikey", "risk": { "class": "review-required", "reasons": ["native cloud: project/location auth and partner-protocol differences need their own proof (WS-13 §5)"] } }
  ],

  "categoryDispositions": {
    "$comment": "WS-13 §1's disposition table, as DATA the extractor applies. `candidate-pool` means \"eligible for the `providers` intake list above\" — never \"included\": an api-key row absent from that list is still rejected, with class `not-allowlisted`. Every other category fails closed at import time, whatever the allowlist says (the check runs BOTH ways: an allowlisted id whose upstream category is not `candidate-pool` fails the run, which is the class transition WS-13 §3 step 7 blocks).",
    "noauth": { "disposition": "blocked", "exclusionClass": "category-no-auth", "reason": "WS-13 §1: reject by default; individually allowlistable only after protocol and terms review" },
    "oauth": { "disposition": "blocked", "exclusionClass": "category-oauth", "reason": "WS-13 §1/§6: generic OAuth import is rejected; only provider-authorized product flows, which are Winter-owned rows, never extracted ones" },
    "web-cookie": { "disposition": "blocked", "exclusionClass": "category-web-cookie", "reason": "WS-13 §1: excluded categorically — browser-session transport" },
    "apikey": { "disposition": "candidate-pool", "exclusionClass": "not-allowlisted", "reason": "WS-13 §1: the main source pool, curated by LLM relevance and adapter quality — presence upstream is never inclusion" },
    "local": { "disposition": "winter-owned", "exclusionClass": "category-local-live-discovery", "reason": "WS-13 §12: the twelve chat/self-hosted backends are Winter-owned rows on live discovery (machine-specific inventories); image-only systems are excluded from worker models" },
    "search": { "disposition": "blocked", "exclusionClass": "category-search", "reason": "WS-13 §1: not an LLM provider" },
    "audio": { "disposition": "blocked", "exclusionClass": "category-audio", "reason": "WS-13 §1: not an Agent SDK worker-model provider" },
    "upstream-proxy": { "disposition": "blocked", "exclusionClass": "category-upstream-proxy", "reason": "WS-13 §1: no proxy-of-proxy layer" },
    "cloud-agent": { "disposition": "blocked", "exclusionClass": "category-cloud-agent", "reason": "WS-13 §1: remote agent products, not model providers" },
    "system": { "disposition": "blocked", "exclusionClass": "category-system", "reason": "WS-13 §1/§9: `auto` is routing policy, which this layer bans outright" }
  },

  "blocked": [
    { "upstreamId": "*", "reason": "Any upstream row whose transport is a browser session, a private/undocumented API, or a consumer subscription is blocked categorically and is never promotable by this file alone (WS-13 §5/§6)." },
    { "upstreamId": "comfyui", "reason": "Image-generation system in the `local` category — WS-13 §1 keeps it out of selectable worker models (report §4.2)." },
    { "upstreamId": "sdwebui", "reason": "Image-generation system in the `local` category — WS-13 §1 keeps it out of selectable worker models (report §4.2)." }
  ],

  "notes": [
    "`codex-oauth` is NOT extracted from upstream — it is a Winter-owned provider carried over per D11 with originator \"winter\" (WS-01 §3), and its descriptor's `upstream.project` is \"winter\".",
    "The twelve local OpenAI-compatible ids (WS-13 §12) are likewise Winter-owned: their inventories are machine-specific and always come from live discovery, so there is no upstream row to extract.",
    "Promoting a provider from `blocked`/`review-required` to `approved`, or adding an id to `providers`, is the reviewed allowlist change WS-13 §13 requires before anything can reach `supported`. The extractor has no other door."
  ]
}
